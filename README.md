# Garden Boss — IoT Hub

Dashboard web per la gestione remota delle elettrovalvole del sistema di irrigazione **Garden Boss** (ESP32).

Si collega via MQTT-over-WebSocket (TLS) a HiveMQ Cloud: inserisci host del broker, username e password nella schermata di login.

> Interfaccia statica — nessun dato viene salvato lato server.
